---
import Section from "./section.astro";
---
<Section
  title="Descuentos"
  subtitle="Descuentos todos los dias"
  color="black"
  id="Descuentos"
>
  <div slot="footer" class="gap-x-6 flex mx-auto">
    <a
      class="border-[3px] border-gray-600 bg-gray-600 text-sm rounded font-medium text-white px-12 py-2 inline-block transition-colors hover:border-gray-500 hover:bg-gray-500"
      href="#"
    >
      Productos</a
    >
    <a
      class="bg-white text-sm rounded font-medium px-12 py-2 transition-colors hover:bg-black hover:text-white inline-flex justify-center items-center text-black"
      href="#"
    >
      Servicios</a
    >
  </div>

  <img
    slot="background"
    alt="Descuentos"
    class="h-full w-full object-cover object-center"
    src="public/Descuentos.jpeg" 
    id="descuentospage"
  />
</Section>

<script>
  function isEdge() {
    if (
      navigator.userAgent.indexOf("Edge") > -1 ||
      navigator.userAgent.indexOf("Edg") > -1
    ) {
      return true;
    } else {
      return false;
    }
  }

  let imageElement = document.querySelector("#descuentospage") as HTMLImageElement; // TypeScript

  if (isEdge()) {
    imageElement.src = "Descuentos.webp";
  } else {
    imageElement.src = "Descuentos.jpeg";
  }
</script>