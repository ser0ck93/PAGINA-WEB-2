---
import FooterMenu from "./FooterMenu.astro";
const {color, title, subtitle,showFooterContent} = Astro.props
const textColor = `text-${color}`

---
<section 
class="bg-black h-screen w-screen text-center overflow-hidden z-30 relative"
data-header-color={color}
id="{id}"
>

<div class="z-30 relative h-full flex flex-col"> 

 <header>
    <h2 class={`text-${color} pt-40 text-[40px] font-medium`}>
        {title} </h2>
    <p class={`text-${color} text-sm`}>
       {subtitle} 
    </p>
 </header>
    
 <footer class="flex flex-col flex-grow justify-end pb-20">
    <slot name="footer" /> 
    <!-- Contidionally load footer: --> 
    {showFooterContent && <FooterMenu />}
 </footer>
</div>

<div class="absolute top-0 bottom-0 h-full w-full z-10">
<slot name="background"></slot>
</div>   

</section>